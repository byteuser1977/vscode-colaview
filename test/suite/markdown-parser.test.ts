// Markdown 解析器单元测试 — 模块已迁移至 @bytechain.cn/colamd，测试暂时跳过
import * as assert from 'assert';

suite('Markdown Parser', () => {
    test('placeholder — parser migrated to @bytechain.cn/colamd', () => {
        assert.ok(true, 'Test suite retained for future integration tests');
    });
});